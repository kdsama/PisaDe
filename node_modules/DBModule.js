var sql = require("./mysql");
var client =sql.createConnection({
	host:"localhost",
	user:"root",
	password:"password"
});
client.connect();
console.log("connected");
client.query("use details",function(err){
	if(err){
		console.log("not present")
	}
	else{
		console.log("present");
	}
});


exports.authenticateUser = function(username,password,callback) {
	var strQuery = "select * from logindetails where name ='"+username+"'and password='"+password+"';";
	client.query(strQuery,function(err,rows){
		console.log(rows);
     if(rows.length==0){
			console.log("invalid length");
			return callback(err);
		}
		else{
			return callback(null,"success");
		}
	})
                                             }
									exports.addUser = function(username, password, address, res) {           

     return "User added successfully";

}